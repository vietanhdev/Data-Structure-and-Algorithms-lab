#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    if (argc != 3) {
		printf("Error input!!\n");
		return 1;
	}

	int n = atoi(argv[2]);
	for (int i = 0; i < n; i++) {
		printf("%s ", argv[1]);
	}
    return 0;
}
